{% extends 'admin/layout/base.html.twig' %}

{% block title %} Dashboard Users {% endblock %}

{% block body %}

    <div class="content-wrapper">
        {% set isSuperAdmin = is_granted('ROLE_SUPER_ADMIN') %}
        <div class="content-header">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-6">
                        <h5 class="text-dark mt-1">
                            {{ isSuperAdmin ? 'Users overview' : 'User overview' }}
                        </h5>
                    </div>
                    <div class="col-sm-6">
                        <ol class="breadcrumb text-end">
                            <!-- <li class="text-secondary pointer d-btn">
                                <span class="md-d-none pr-1">Infos</span>
                                <span class="fa fa-info-circle fa-sm"></span>
                            </li> -->
                        </ol>
                    </div>
                </div>
            </div>
        </div>

        <section class="content">
            <div class="container-fluid area-section">

                <div class="row py-3">
                    <div class="col-lg-12">
                        {% include 'admin/users/partial/table_content.html.twig' %}
                    </div>
                </div>

            </div>
        </section>
    </div>

{% endblock %}